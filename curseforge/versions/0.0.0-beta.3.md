## 0.0.0-beta.3 — Bugfix Release

**Fixes:**
- Fixed crash on player tick: `ModParticles` static initializer ran before `particles` list was created (NullPointerException)
- Fixed `ClassCastException` when syncing server config to client: boolean values were incorrectly read as integers
- Fixed `Invalid path` error for particles atlas: corrected directory source path format

**Technical:** Requires NeoForge 26.1.2.78+ / Minecraft 26.1.2
