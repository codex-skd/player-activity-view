<h2>v0.0.0-beta.11 — Particle textures fixed for 1.21.4 atlas</h2>

<h3>Fix</h3>
<ul>
<li><strong>Particle indicators now visible</strong>: Custom textures moved from <code>textures/</code> to <code>textures/particle/</code> so they are included in the 1.21.4 particle atlas.</li>
<li><strong>Sprite initialization</strong>: <code>PlayerActivityClient</code> now loads particle sprites from the atlas at startup and on resource reload.</li>
<li><strong>Crash prevention</strong>: Null-safety check in <code>ParticleRotating</code> prevents crash when sprite is unavailable.</li>
</ul>

<h3>Changed</h3>
<ul>
<li>All particle textures relocated to <code>assets/player_activity_view/textures/particle/</code></li>
<li><code>SpriteInfo</code> paths updated with <code>particle/</code> prefix</li>
</ul>
