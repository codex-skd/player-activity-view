<h2>v0.0.0-beta.14 — Crop screen mirror to the actual menu panel</h2>

<h3>Fix</h3>
<ul>
<li><strong>Screen mirror showed the whole frame</strong>: the live preview shown to other players captured the entire screen (3D world, dark background overlay and all) instead of just your open menu. Now it crops to the real on-screen bounds of the current GUI panel, with a small padding border.</li>
</ul>

<h3>Notes</h3>
<blockquote>If multiple people test this together, make sure everyone is on the same version — mismatched versions between clients can desync the shown status (e.g. always showing the pause/escape icon) and break sending in one direction.</blockquote>
