## 0.0.0-beta.5 — Phase 1: Mixins & Input Events

**Added:**
- PostChainResize mixin: resizes screen particle renderer when window changes
- BlockBehaviorUse mixin: detects block interactions for inventory tracking
- Mouse input events: proper `InputEvent.MouseButton.Post` handler (replaced partial LeftClickEmpty)
- Keyboard input events: `InputEvent.Key` handler for key press detection

**Documentation:**
- Updated WORKFLOW.md with mixin conventions, phased implementation guide, and testing procedures

**Technical:** Requires NeoForge 26.1.2.78+ / Minecraft 26.1.2
