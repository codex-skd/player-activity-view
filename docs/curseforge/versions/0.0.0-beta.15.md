<h2>v0.0.0-beta.15 — Fix red-tinted screen mirror and wrong orientation</h2>

<h3>Fix</h3>
<ul>
<li><strong>Screen mirror showed solid/heavy red</strong>: a rendering coordinate bug accidentally applied the game's "hurt" red-flash overlay to the mirror instead of no overlay.</li>
<li><strong>Screen mirror always faced the viewer</strong>: it now stays locked to the direction the shown player's body is facing (like a tablet held up in front of them), instead of rotating to face whoever is looking at it.</li>
</ul>
